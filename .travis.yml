language: node_js

node:
  - 8
  - 9

cache:
  yarn: true
  directories: 
    - node_modules

stages:
  - "test"
  - ":ship to npm"

jobs:
  include:
    - stage: "test"
      dist: trusty
      script: "yarn test"